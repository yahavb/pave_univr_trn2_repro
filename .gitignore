__pycache__/
*.pyc
# reproducer output
out_*.png
out_*.npy
*.log
univr_microbench_*/
univr_warpop_*/
*.ntff
*.tar.gz

# Downloaded run artifacts. NEVER commit these -- a NEFF is tens of MB and GitHub
# rejects at 100 MB. `git add -A` in this repo committed a 44 MB .neff once; use
# explicit paths.
.DS_Store
*.neff
*.pftrace
*.pftrace.gz
trace_rank*.json
trace_rank*.json.gz
system_profile*.pftrace*
timeline_*.tar.gz
trace_analysis/
results/
upload/
run_*/
frame_*.png
frame_*.npy

# Downloaded run-artifact trees. warpeager_run/ contained a univr-bench-job.yaml that
# `git add -A -- "*.yaml"` swept into a commit on 2026-08-20; the ignore patterns above
# only covered binaries, not the directories.
warpeager_run/
taps_142240/
explorer_ab/
trace_processor
upload/
exp_univer-*/
univr-taps-ab-*
